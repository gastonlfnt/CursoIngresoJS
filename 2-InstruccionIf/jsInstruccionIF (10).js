function Mostrar()
{
	var random
	random=Math.floor(Math.random(11) * 11 - 10 + 10 );
	if (random>=9)
	
	{
		alert ("la nota es 9 Excelente");
	}
	else if	(random<=9)
	
	{
		alert ("Muy bien"); }
}
	else if (random<=6)
	{
		alert ("Bien");
	}
